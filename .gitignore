# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data based and not code based.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not considered part of the co-authored/shared codebase.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include lock file for versioning of providers
# .terraform.lock.hcl

# Include terraform plan outputs
*.tfplan

# Ignore CLI configuration file
.terraformrc
terraform.rc

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
